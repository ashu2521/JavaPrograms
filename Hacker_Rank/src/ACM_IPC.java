import java.io.*;
import java.util.*;
import java.text.*;
import java.math.*;
import java.util.regex.*;

public class ACM_IPC { 
	  public static void main(String[] args) {
	       /* Scanner in = new Scanner(System.in);
	        int n = in.nextInt();
	        int m = in.nextInt();
	        BitSet bit[] = new BitSet[n];
	        for(int topic_i=0; topic_i < n; topic_i++){
	            bit[topic_i] = new BitSet(m);
	            String s = in.next();
	            for(int j = 0; j < m; j++){
	                if(s.charAt(j) == '1')
	                    bit[topic_i].set(j);
	            }
	        }
	        int maxSubKnown = 0;
	        int teamKnowMax = 0;
	        for(int i = 0; i < n-1; i++){
	            for(int j = i+1; j < n; j++){
	                BitSet bitset = (BitSet)bit[i].clone();
	                bitset.or(bit[j]);
	                int knownSub = bitset.cardinality();
	                System.out.println(i + " " + j + " " + knownSub + " " + bit[i] + " " + bit[j]);
	                if(knownSub > maxSubKnown){
	                	maxSubKnown = knownSub;
	                    teamKnowMax = 1;
	                }
	                else if(knownSub == maxSubKnown )
	                    teamKnowMax++;
	            }
	        }*/
	        System.out.println(0%0);
	   //     System.out.println(maxSubKnown);
	     //   System.out.println(teamKnowMax);
	    }
}
